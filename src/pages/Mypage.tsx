import { Button } from "@mui/material";

export default () => {
  return (
    <div>Mypage</div>
    // <div>
    //   {user && Object.keys(user).map(info => {
    //     return <p>{info}:{user[info]}</p>
    //   })}
    // </div>
  );
}